@model Prof_Me.Data.Models.Accomplishment
@using Microsoft.AspNetCore.Identity
@using Prof_Me.Data.Models;

@inject SignInManager<UserData> SignInManager
@inject UserManager<UserData> UserManager
@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Accomplishment</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.CertificateName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.CertificateName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.GotDate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.GotDate)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-controller="UserProfiles" asp-action="MyProfile" asp-route-id="@UserManager.GetUserName(User)">Back to Profile</a>
    </form>
</div>
